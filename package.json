{
  "name": "gemui",
  "version": "1.0.0",
  "private": true,
  "description": "A premium web GUI for Gemini CLI",
  "scripts": {
    "dev": "npm run dev:stop && concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:stop": "node scripts/kill-ports.js 4008 5173",
    "dev:server": "cd server && node --watch server.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && node server.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
